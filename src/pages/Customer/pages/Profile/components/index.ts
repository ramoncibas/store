import ProfileAvatar from "./Avatar";
import ProfileForm from "./Form";
import ProfileAvatarPreview from "./Preview";

const Profile = {
  Avatar: ProfileAvatar,
  Preview: ProfileAvatarPreview,
  Form: ProfileForm
};

export default Profile;
